import type { AppManifestItem } from "../manifest-types";

const manifest: AppManifestItem = {
  slug: "give-birth-to-roadmap",
  name: "Give Birth to Roadmap",
  description: "All pregnancy guidance in one app.",
  status: "live",
  tags: ["Core", "Roadmap", "Care"],
};

export default manifest;
